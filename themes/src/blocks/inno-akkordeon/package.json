{
	"name": "inno-akkordeon",
	"version": "0.1.0",
	"description": "Example block scaffolded with Create Block tool.",
	"author": "The WordPress Contributors",
	"license": "GPL-2.0-or-later",
	"main": "build/index.js",
	"scripts": {
		"start": "run-p watch:build watch:copy",
		"watch:build": "wp-scripts start",
		"watch:copy": "cpx \"build/**/*\" ../../../dist/blocks/inno-akkordeon --watch --clean --verbose",

		"build": "wp-scripts build && npm run export",
		"export": "cpx \"build/**/*\" ../../../dist/blocks/inno-akkordeon --clean",

		"format": "wp-scripts format",
		"lint:css": "wp-scripts lint-style",
		"lint:js": "wp-scripts lint-js",
		"packages-update": "wp-scripts packages-update",
		"plugin-zip": "wp-scripts plugin-zip"
	},
	"devDependencies": {
		"@wordpress/scripts": "^30.27.0"
	}
}
